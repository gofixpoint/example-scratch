{"linear_ticket_url":"https://linear.app/fixpoint/issue/KAPRO-252/move-environment-variables-section","github_pr_url":"https://github.com/gofixpoint/example-repo/pull/37","linear_text":"Move environment variables section\n\nIn the example-repo, move the environment variables section from the bottom of the  page to right below the first heading block on the page.","change_summary":"In src/App.tsx, moved the `env-vars` <section> (Environment Variables heading + VITE_FRONTEND_URL/VITE_FRONTEND_PORT list) from the bottom of the page shell to directly after the closing </header> of the hero, so it renders below the first heading block. No other changes."}
